/* eslint-disable no-unused-vars */
export const AdminDashboardExam = class AdminDashboardExam {
    constructor(options, app) {
        this.options = options || {};
        this.app = app;
    }

    async find(params) {
        return [];
    }
};
